@model IEnumerable<GameShop.DATA.EF.ProductStatus>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.StatusName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.StatusDescription)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.StatusName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.StatusDescription)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ProductStatusID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ProductStatusID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ProductStatusID })
        </td>
    </tr>
}

</table>
